// gather inputs

var billTotal = 100;
var tipPercent = 15;

// perform operation
var tipAmount = tipPercent * (billTotal / 100);

// print output

console.log("The tip on a $", billTotal, "food bill is $", tipAmount);
